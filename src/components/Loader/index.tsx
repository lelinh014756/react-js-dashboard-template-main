import LinearProgress from '@mui/material/LinearProgress';

function index() {
  return (
    <div className="fixed top-0 left-0 z-[13] w-full">
      <LinearProgress color="primary" />
    </div>
  );
}

export default index;
